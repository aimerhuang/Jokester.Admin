# 后台管理系统设计书

## 1. 项目概述

### 1.1 项目背景

当前需要建设一套基于 **.NET 10 Web API** 的后台管理系统，用于统一管理多个业务站点，并实现用户、页面、功能权限的集中控制。系统采用前后端分离架构，后端负责认证、授权、业务处理与数据管理，前端负责界面展示与交互。

现有业务站点包括：

1. 个人博客网站：支持文章增删改查、发布管理
2. GPT 生图网站：管理生图功能，无权限用户不可执行生图操作
3. 未来新增站点：后台需支持快速扩展与接入

### 1.2 项目目标

建设一套可扩展的后台管理平台，实现：

- 多站点统一管理与入口
- 用户统一登录认证（JWT）
- 页面级、按钮级、接口级三层权限控制
- 支持后续新增站点、页面、功能权限
- 支持灵活的用户、角色、权限分配
- 完整的操作审计能力

### 1.3 项目定位

本系统定位为统一权限与后台运营管理平台，既是各网站的后台入口，也是权限中心和资源管理中心。

## 2. 建设范围

### 2.1 本期范围

| 模块 | 内容 |
|------|------|
| 用户管理 | 用户增删改查、禁用启用 |
| 角色管理 | 角色增删改、权限分配 |
| 站点管理 | 站点配置与用户站点授权 |
| 菜单/页面管理 | 菜单树维护、权限节点管理 |
| 认证授权 | JWT 登录、Token 刷新、权限校验 |
| 博客管理 | 文章增删改查、发布下线、媒体资源管理 |
| GPT 生图管理 | 生图任务发起、记录查询 |
| 日志审计 | 登录日志、操作日志 |

### 2.2 后续扩展范围

- 用户直接授权
- 部门/组织架构
- 数据权限控制
- 更完整的风险评分与 WAF/反自动化治理
- 多租户能力

## 3. 总体架构设计

### 3.1 技术选型

| 层次 | 技术 | 备注 |
|------|------|------|
| 前端框架 | Vue 3 + Vite | 动态菜单、权限指令 |
| 后端框架 | .NET Web API | `.NET 10` |
| 数据库 | MySQL | 8.0 / utf8mb4 |
| 缓存 | Redis | Token、权限缓存 |
| 认证 | JWT | AccessToken + RefreshToken |
| 对象存储 | 本地文件 / 可扩展 OSS、MinIO | 博客媒体资源 |
| API 文档 | Swagger | 开发环境启用 |

### 3.2 后端分层架构

- `Presentation`：Controllers、Middleware、Filters
- `Application`：Services、DTOs、权限判断
- `Domain`：Entities、枚举、规则
- `Infrastructure`：SqlSugar、Mapster、Redis、日志

### 3.3 模块划分

1. Auth
2. User
3. Role
4. Site
5. Menu
6. Blog
7. AiImage
8. Log

## 4. 权限模型设计

### 4.1 权限模型

采用 **RBAC + 站点维度** 模型：

- 用户绑定角色，角色绑定菜单/按钮/接口权限
- 用户绑定可访问站点
- 超级管理员（`is_super_admin=1`）绕过所有权限校验

### 4.2 权限控制三层

| 层级 | 控制粒度 | 示例 |
|------|----------|------|
| 站点级 | 用户是否可进入某网站后台 | 可访问博客后台，不可访问 GPT 生图后台 |
| 页面级 | 用户是否可访问某页面 | 可进文章管理页，不可进用户管理页 |
| 操作级 | 用户是否可执行具体功能 | 可新增文章，不可删除文章 |

### 4.3 权限编码规范

格式：`系统.模块.动作`

```text
Blog.Article.View
Blog.Article.Create
Blog.Article.Update
Blog.Article.Delete
Blog.Article.Publish

Blog.Media.View
Blog.Media.Upload
Blog.Media.Delete

Blog.Comment.View
Blog.Comment.Review
Blog.Comment.Delete

Blog.Dashboard.View

AiImage.Page
AiImage.Generate
AiImage.Record.View
AiImage.Record.Delete
```

### 4.4 菜单类型定义

| `menu_type` | 含义 | 说明 |
|-------------|------|------|
| 1 | 目录 | 导航分组 |
| 2 | 页面菜单 | 具有路由与前端组件 |
| 3 | 按钮权限 | 页面内按钮权限 |
| 4 | 接口权限 | 后端接口鉴权节点 |

## 5. 业务流程设计

### 5.1 登录流程

1. 用户提交账号密码
2. 后端校验账号状态与密码
3. 生成 AccessToken + RefreshToken
4. 返回用户信息、可访问站点、权限列表
5. 前端存储 Token 并渲染菜单

### 5.1.1 注册邮箱验证码流程

1. 前端调用 `POST /api/auth/register/email-code`，请求体只传 `email`
2. 后端执行邮箱验证：始终检查格式、长度和黑名单域名；当 `EmailValidation.EnableApiValidation=true` 时再调用第三方邮箱验证 API
3. 后端生成 6 位邮件验证码，按规范化后的邮箱写入 Redis，有效期 10 分钟
4. 后端通过 `Mail` 配置的 SMTP 服务发送验证码邮件
5. 前端调用 `POST /api/auth/register`，请求体只传 `email`、`emailCode`、`password`
6. 后端用同一个 `email` 查询 Redis 验证码，校验通过后创建用户并删除验证码键
7. 后端按规范化邮箱 `@` 前的账号部分生成 `userName` 和 `nickName`；用户名冲突时自动消歧，用户可直接使用邮箱登录

### 5.2 Token 刷新策略

- AccessToken：10–15 分钟（当前推荐 15 分钟，固定校验 `iss`、`aud`、`HS256`，包含 `jti` 与 `sid`）
- RefreshToken：7 天
- RefreshToken 默认通过哈希键存储于 Redis，使用 session/token family 和 Lua 原子消费；并发刷新仅一个成功，重放会撤销整个 family
- 登出、禁用、改密和安全上下文降权会撤销 Refresh Token；既有 Access Token 最迟在 15 分钟内自然失效

### 5.3 权限校验流程

1. 请求进入
2. JWT 中间件校验 Token
3. 权限中间件读取接口声明的 `permission_code`
4. 优先从 Redis 读取用户权限
5. 不命中时回退数据库
6. 不具备权限则返回 403

### 5.4 多站点切换流程

1. 登录后获取可访问站点列表
2. 前端按站点显示导航
3. 切换站点后加载该站点菜单树
4. 业务表使用 `site_id` 标记归属

### 5.5 博客站点固定路由约束

当前博客后台虽然沿用站点模型，但接口层按“单博客站点”收敛：

- 博客文章和媒体接口固定归属 `siteCode=blog`
- 文章新增/编辑、文章列表、媒体上传、媒体列表均不再要求调用方传 `siteId`
- 后端在服务层解析 `blog` 站点 ID 后，再执行数据写入和查询过滤
- 数据表中的 `site_id` 仍保留，作为数据归属字段和后续多站点扩展基础

### 5.6 博客媒体与缩略图流程

1. 上传媒体写入 `blog_media`，返回可插入正文的 `url`
2. 创建或更新文章时，后端解析正文 HTML 中的 `<img src="...">`
3. 解析出的 URL 匹配 `blog_media.url` 后，写入 `blog_article_media`
4. 文章列表和详情返回 `thumbnailUrl`，优先级为 `cover_url`、文章媒体关联第一张图、正文第一张图
5. 文章正文仍返回 HTML 内容，由前端按 HTML 渲染图片和段落

### 5.7 移动端合规与 StoreKit 流程

- 隐私政策、服务条款和 AI 数据处理声明由独立法律文档系统提供；注册不提交或校验这些版本，第三方 AI 生图仍必须校验当前 AI processing 告知与 Provider 授权。
- Apple 套餐和积分只能由服务端商品映射决定。交易 JWS 验签、幂等履约、积分入账和流水写入保持同一事务；Server Notifications V2 验签后按通知 UUID 幂等处理。
- 账户删除先撤销全部会话，再由后台 Worker 清理私有数据并匿名化必须保留的财务/审计主体；数据删除与通知重试互不重复执行。
- 新移动端统一通过 `POST /api/ai/images` 按 `modelCode` 创建任务，并使用当前用户拥有的 Asset ID 引用图片；旧 URL 字段和模型专用路由只保留兼容期。

完整接口契约见 `docs/integration-guide.md`，状态机和数据边界见 `docs/architecture.md`，部署与验收前置条件见 `docs/runbook.md`。

## 6. 数据库设计原则

### 6.1 设计规范

- 主键统一 `BIGINT`
- 逻辑删除统一 `is_deleted`
- 核心表统一审计字段
- 高频查询字段建立索引
- 核心关联使用外键

### 6.2 通用审计字段

```sql
created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
created_by BIGINT DEFAULT NULL,
updated_at DATETIME DEFAULT NULL,
updated_by BIGINT DEFAULT NULL,
is_deleted TINYINT(1) NOT NULL DEFAULT 0
```

## 7. 数据库表结构设计

### 7.1 `sys_user`

用户基础信息、登录状态、超级管理员标记。

### 7.2 `sys_role`

角色信息与角色编码，如 `blog_admin`。

### 7.3 `sys_site`

站点信息与站点编码，如 `blog`、`ai_image`。

### 7.4 `sys_menu`

菜单、按钮、接口权限统一建模，按 `site_id` 归属站点。

### 7.5 关联表

- `sys_user_role`
- `sys_role_menu`
- `sys_user_site`
- `blog_article_media`

### 7.6 `blog_article`

博客文章表，关键字段：

- `site_id`
- `title`
- `summary`
- `content`
- `cover_url`
- `tags`
- `status`：0 草稿，1 发布，2 隐藏
- `view_count`

说明：

- 当前实现仍保留 `site_id` 外键
- 但博客后台接口不再让前端传 `siteId`
- 文章新增/编辑接口允许通过 `status` 设置状态
- `site_id` 由后端根据 `siteCode=blog` 自动写入
- 列表和详情接口返回 `coverUrl` 与 `thumbnailUrl`
- `thumbnailUrl` 优先使用 `cover_url`，其次使用 `blog_article_media` 关联的第一张媒体图，最后从正文第一张图片兜底提取

### 7.7 `blog_media`

博客媒体资源表，关键字段：

- `site_id`
- `file_name`
- `storage_key`
- `url`
- `mime_type`
- `file_size`
- `storage_provider`

说明：

- 当前实现仍保留 `site_id` 外键
- 上传和列表查询均固定绑定 `blog` 站点

### 7.8 `blog_article_media`

文章媒体关联表，用于追踪文章正文引用了哪些已上传媒体资源：

- `article_id`
- `media_id`
- `created_at`

说明：

- 创建或更新文章时由后端根据正文 `<img src="...">` 自动同步
- 仅当图片 URL 能匹配 `blog_media.url` 时写入关联
- 缩略图逻辑会优先读取该表关联的第一张媒体图

### 7.9 `ai_image_parameter`

生图参数表，按 `param_type` 区分：

- `resolution`：分辨率档位，`1k/2k/4k` 对应长边 `1024/2048/3840`；GPT 实际宽高均为 `16px` 倍数且不超过 `3840`，长短边比例不超过 `3:1`，总像素限制为 `655,360` 到 `8,294,400`
- `quality`：质量档位，`low/med/high`，其中 `med` 的供应商参数为 `medium`
- `aspect_ratio`：legacy GPT 画幅比例支持 `1:1`、`16:9`、`9:16`、`4:3`、`3:4`、`3:2`、`2:3`、`21:9`，必须显式选择且不接受 `auto`；`size-mode-v1` auto 不从该目录扩展能力，Nano Banana2 仍可使用 legacy `auto`

### 7.10 `ai_image_task`

生图任务记录表，按 `site_id` 标识归属。legacy 任务保存用户选择的 `resolution_code`、`quality_code`、`aspect_ratio_code` 及计算尺寸；`size-mode-v1` 任务另保存 `model_code`、不可变 release/price、`size_mode`、可空请求尺寸、真实输出尺寸、稳定失败字段和 claim fencing。auto 的分辨率、比例和请求宽高为 NULL，不能用 legacy 默认值代填。

版本化模型目录由 `ai_image_model_release`、current pointer、route/price 明细组成；durable 幂等请求、有序任务批次、输入快照、结果事实、Provider attempt 和事务 Outbox 分别保存在独立表。创建批次、积分预留和 Outbox 同一事务提交，Worker 按冻结 release 与 claim epoch 执行，未知 Provider 结果隔离到 `reconcile_by` 后统一退款。

### 7.11 `sys_login_log`

登录日志。

### 7.12 `sys_operation_log`

操作日志。

## 8. 表关系设计

### 8.1 核心关系

- `sys_user` -> `sys_user_role` -> `sys_role`
- `sys_role` -> `sys_role_menu` -> `sys_menu`
- `sys_user` -> `sys_user_site` -> `sys_site`
- `sys_site` -> `blog_article`
- `sys_site` -> `blog_media`
- `blog_article` -> `blog_article_media` -> `blog_media`
- `sys_site` -> `ai_image_task`
- `ai_image_parameter` 为生图参数字典表

### 8.2 说明

- `sys_menu` 通过 `site_id` 归属 `sys_site`
- `blog_article`、`blog_media`、`ai_image_task` 均保留 `site_id`
- 当前只有博客接口在应用层固定绑定 `siteCode=blog`

## 9. 接口设计规范

### 9.1 统一响应格式

```json
{
  "code": 0,
  "message": "success",
  "requestId": "0HN...",
  "data": {}
}
```

失败响应使用稳定的字符串机器码，避免客户端依赖文案：

```json
{
  "code": "VALIDATION_ERROR",
  "message": "The request is invalid.",
  "requestId": "0HN...",
  "details": null
}
```

分页结构：

```json
{
  "total": 100,
  "pageIndex": 1,
  "pageSize": 20,
  "items": [],
  "hasMore": true
}
```

### 9.2 常见错误码

| code | 含义 |
|------|------|
| `0` | 成功 |
| `VALIDATION_ERROR` | 参数或模型校验失败 |
| `UNAUTHORIZED` / `ACCESS_TOKEN_EXPIRED` | 未登录、令牌失效或需刷新 |
| `FORBIDDEN` | 无权限 |
| `NOT_FOUND` | 资源不存在或对当前用户不可见 |
| `RATE_LIMITED` | 请求受限；HTTP 429 同时返回 `Retry-After` 和 `details.retryAfterSeconds` |
| `SERVICE_UNAVAILABLE` | 当前依赖或必要服务不可用 |

### 9.3 路径约定

- 前缀：`/api`
- 标准 CRUD：`GET/POST/PUT/DELETE`
- 非 CRUD 操作：如 `POST /api/blog/articles/{id}/publish`

### 9.4 认证接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register/email-code` | 发送注册邮箱验证码 |
| POST | `/api/auth/register` | 邮箱验证码注册 |
| POST | `/api/auth/login` | 登录 |
| POST | `/api/auth/refresh` | 刷新 AccessToken |
| POST | `/api/auth/logout` | 登出 |
| GET | `/api/auth/profile` | 当前用户信息 |

注册请求说明：
- `POST /api/auth/register/email-code` 请求体只包含 `email`，不需要图片验证码
- `POST /api/auth/register` 请求体只包含 `email`、`emailCode`、`password`
- 后端按规范化邮箱 `@` 前的账号部分生成 `userName` 和 `nickName`，用户名冲突时自动消歧
- 登录接口支持使用邮箱和密码登录
- `emailCode` 由后端生成并通过 SMTP 邮件发送，前端不应自行生成
- 发送验证码与注册时传入的 `email` 必须指向同一个邮箱

### 9.5 用户与角色接口

| 方法 | 路径 |
|------|------|
| GET | `/api/users` |
| POST | `/api/users` |
| PUT | `/api/users/{id}` |
| PUT | `/api/users/{id}/status` |
| DELETE | `/api/users/{id}` |
| GET | `/api/roles` |
| POST | `/api/roles` |
| PUT | `/api/roles/{id}` |
| PUT | `/api/roles/{id}/menus` |
| PUT | `/api/roles/{id}/status` |
| DELETE | `/api/roles/{id}` |

### 9.6 站点与菜单接口

| 方法 | 路径 |
|------|------|
| GET | `/api/sites` |
| GET | `/api/sites/site_code` |
| POST | `/api/sites` |
| PUT | `/api/sites/{id}` |
| PUT | `/api/sites/{id}/status` |
| DELETE | `/api/sites/{id}` |
| GET | `/api/menus/tree` |
| GET | `/api/menus` |
| POST | `/api/menus` |
| PUT | `/api/menus/{id}` |
| PUT | `/api/menus/{id}/status` |
| DELETE | `/api/menus/{id}` |

说明：`GET /api/sites/site_code` 为公开接口，不需要授权，返回所有未删除站点及 `status` 状态。

### 9.7 博客接口

| 方法 | 路径 | 权限码 | 说明 |
|------|------|--------|------|
| GET | `/api/blog/articles` | `Blog.Article.View` | 文章分页 |
| GET | `/api/blog/articles/{id}` | `Blog.Article.View` | 文章详情 |
| POST | `/api/blog/articles` | `Blog.Article.Create` | 新增文章，支持 `status` |
| PUT | `/api/blog/articles/{id}` | `Blog.Article.Update` | 编辑文章，支持修改 `status` |
| DELETE | `/api/blog/articles/{id}` | `Blog.Article.Delete` | 删除文章 |
| POST | `/api/blog/articles/{id}/publish` | `Blog.Article.Publish` | 发布 |
| GET | `/api/blog/media` | `Blog.Media.View` | 媒体分页 |
| POST | `/api/blog/media/upload` | `Blog.Media.Upload` | 上传媒体 |
| DELETE | `/api/blog/media/{id}` | `Blog.Media.Delete` | 删除媒体 |
| GET | `/api/blog/comments/captcha` | Anonymous | 获取评论图片验证码，返回 `captchaId`、`imageBase64`、`mimeType`、`expiresInSeconds` |
| POST | `/api/blog/comments/public` | Anonymous | 公开提交评论，必须传 `captchaId` 与 `captchaAnswer`，新评论默认待审核 |
| GET | `/api/blog/comments/public` | Anonymous | 公开查询已通过评论，只返回 `status=1` |
| GET | `/api/blog/comments` | `Blog.Comment.View` | 后台评论分页，支持 `articleId`、`parentId`、`status`、`keyword` |
| PUT | `/api/blog/comments/{id}/review` | `Blog.Comment.Review` | 审核评论，`status` 只能为 1 已通过、2 已拒绝、3 垃圾评论 |
| DELETE | `/api/blog/comments/{id}` | `Blog.Comment.Delete` | 后台删除评论 |
| GET | `/api/blog/dashboard/stats` | `Blog.Dashboard.View` | 博客仪表盘统计，返回文章、评论、媒体统计和最新待审核评论 |

补充说明：

- 上述接口当前均固定绑定 `siteCode=blog`
- 调用方不再传 `siteId`
- 后端内部会将请求限定到 `blog` 站点
- 文章列表和详情返回 `coverUrl` 与 `thumbnailUrl`；`thumbnailUrl` 优先使用封面图，其次使用文章媒体关联第一张图，最后从正文第一张图片兜底提取。
- 创建或更新文章时，后端会解析正文中的 `<img src="...">` 并同步维护 `blog_article_media`。
- 评论状态：0=待审核，1=已通过，2=已拒绝，3=垃圾评论；公开列表只展示已通过评论。
- 图片验证码当前为服务端生成的 6 位大写字母/数字 SVG，答案存储 5 分钟并一次性校验；评论提交和登录失败后的二次验证共用该接口，多实例部署时应确保验证码存储使用共享 Redis。注册邮件发送不使用图片验证码。

### 9.8 积分与 AI 生图接口

| 方法 | 路径 | 权限码 | 说明 |
|------|------|--------|------|
| GET | `/api/points/balance` | 登录用户 | 查询当前用户积分余额和今日签到状态 |
| GET | `/api/points/details` | 登录用户 | 分页查询当前用户积分流水 |
| POST | `/api/points/sign-in` | 登录用户 | 每日签到领取 25 积分；同一自然日只能成功一次 |
| GET | `/api/points/recharge/packages` | 登录用户 | 查询 `monthly/trial/basic/value` 四档套餐；iOS 仅返回已映射商品 |
| POST | `/api/points/recharge/orders` | 登录用户 | 创建 Web/Android 待支付订单，不直接增加积分 |
| POST | `/api/points/recharge/redeem` | 登录用户 | 一次性核销兑换码并原子增加积分 |
| POST | `/api/points/recharge/admin/codes` | 超级管理员 | 按套餐或自定义积分签发兑换码 |
| GET | `/api/ai/images` | `AiImage.Page` | AI 生图记录分页 |
| GET | `/api/ai/images/models` | `AiImage.Generate` | 获取启用的 AI 图片模型 |
| GET | `/api/ai/images/parameters` | `AiImage.Generate` | 获取分辨率、质量、画幅比例参数选项和启用的积分价格 |
| GET | `/api/ai/images/pricing-options` | `AiImage.Generate` | legacy 返回扁平价格；新协议按 `modelCode + catalogVersion` 返回不可变 release 价格 envelope |
| POST | `/api/ai/images/parameters/resolve` | `AiImage.Generate` | 将 `resolutionCode`、`qualityCode`、`aspectRatioCode` 解析为实际 `width`、`height`、`size` 和供应商质量参数 |
| POST | `/api/ai/images` | `AiImage.Generate` | 移动端统一后台任务入口，按 `modelCode` 选择 Provider；参考图优先使用 `referenceAssetIds` |
| POST | `/api/ai/images/generate` | `AiImage.Generate` | 兼容的 GPT Image2 直接生成入口，支持 Asset ID 和受限的同源私有 URL |
| POST | `/api/ai/images/nanoBananaImage/generate` | `AiImage.Generate` | 兼容的 Nano Banana2 直接生成入口；参考图优先使用 `imageAssetIds` |
| POST | `/api/ai/images/nanoBananaImage` | `AiImage.Generate` | 已标记 Deprecated 的 Nano Banana2 后台任务兼容入口 |
| POST | `/api/ai/images/upload` | 登录用户 | 上传、清洗并创建当前用户的私有 Asset |
| GET | `/api/assets/{assetId}/content` | 登录用户 | 鉴权读取当前用户 Asset 原图 |
| GET | `/api/assets/{assetId}/thumbnail` | 登录用户 | 鉴权读取当前用户 Asset 缩略图 |
| DELETE | `/api/assets/{assetId}` | 登录用户 | 所有者软删除 Asset 并清理原图和缩略图 |
| GET | `/api/ai/images/{id}` | `AiImage.Record.View` | 详情 |
| POST | `/api/ai/images/{id}/favorite` | 登录用户 | 收藏或取消收藏结果图片 |
| DELETE | `/api/ai/images/{id}` | `AiImage.Record.Delete` | 删除记录 |

注册成功自动赠送 50 积分。充值套餐固定为 `monthly`、`trial`、`basic`、`value`；其中 `monthly` 到账 5000 积分并在 30 天后到期，其余三档永久有效。生图先扣限时套餐积分，再扣签到积分和永久积分；失败或超时按原批次、原到期时间返还。`monthly` 到账还会创建独立的 `monthly_vip` 权益，不能从积分余额推断，Apple 退款只撤销对应交易来源。充值、批次、会员权益和退款细节以 [docs/point-recharge.md](docs/point-recharge.md) 为准，内部数据流以 [docs/architecture.md](docs/architecture.md) 为准。

legacy 生图从 `ai_image_point_price` 查询价格：GPT Image2 按 `modelCode + resolutionCode + qualityCode` 匹配，Nano Banana2 只按 `modelCode + resolutionCode` 匹配。`size-mode-v1` 改为在创建事务内锁定 `modelCode + catalogVersion` 的 release 价格；explicit 包含分辨率，auto 使用独立无分辨率价格。两者均扣除 `points * imageCount`，且不按最终输出尺寸改价。

分辨率档位按长边计算：`1k=1024`、`2k=2048`、`4k=3840`。GPT 显式尺寸宽高均为 `16px` 倍数且不超过 `3840`，长短边比例不超过 `3:1`，总像素必须在 `655,360` 到 `8,294,400` 之间；典型结果包括 `1k + 1:1 = 1024x1024`、`2k + 16:9 = 2048x1152`、`4k + 1:1 = 2880x2880`、`4k + 16:9 = 3840x2160`。legacy GPT 不接受 `aspectRatioCode=auto`；新协议只通过 `sizeMode=auto + catalogVersion` 使用已审批的独立 auto 路由/价格。Nano Banana2 仍保留 legacy auto。

GPT 主备路由均启用时先调用主路由，`OpenAI.PrimaryTimeoutSeconds` 默认 `180` 秒；主路由超时或失败后切换同一模型与分辨率槽位的备用路由。

GPT Image2 与 Nano Banana2 的参考图、生成图保存在非静态目录 `private-media/ai`，下载必须携带 JWT 并通过所有权校验。新客户端先上传图片并使用返回的 Asset ID；后端在入队前校验所有权、删除状态和文件存在性。旧 `referenceImageUrls`、`maskImageUrl`、`imageUrls` 仅接受当前用户可访问的 `/api/media/ai/...` 同源私有地址，不接受任意远程 URL。参考图合计最多 6 张；有参考图时调用图生图，无参考图时调用文生图。公开博客图片与头像分别使用独立的 `/blog`、`/avatar` 静态前缀。

Nano Banana2 不使用单独的配置段，也不参与 GPT 主备路由切换；文生图和图生图始终读取 `ai_image_model_config` 中当前启用的渠道、Provider 模型、请求路径和 Key。

### 9.9 移动端、法律、账户删除与 Apple 接口

| 方法 | 路径 | 访问策略 | 说明 |
|------|------|----------|------|
| GET | `/api/mobile/config` | Anonymous | 返回维护状态、版本要求、功能开关和当前法律文档版本 |
| GET | `/api/legal/documents/current` | Anonymous | 按平台与语言返回当前已生效法律文档 |
| GET | `/api/users/me/consents` | 登录用户 | 查询当前用户授权状态 |
| PUT | `/api/users/me/consents/ai-processing` | 登录用户 | 接受或撤回第三方 AI 数据处理授权 |
| POST | `/api/auth/account-deletion/requests` | 登录用户 | 创建账户删除申请并撤销会话 |
| GET | `/api/auth/account-deletion/requests/current` | 登录用户 | 查询当前账户删除申请 |
| DELETE | `/api/auth/account-deletion/requests/{requestId}` | 登录用户 | 在允许窗口内撤销删除申请 |
| GET | `/api/points/recharge/packages?platform=ios` | 登录用户 | 返回服务端维护的 Apple Product ID，不返回外部购买链接 |
| POST | `/api/points/recharge/apple/transactions` | 登录用户 | 验证并幂等履约 StoreKit 交易 |
| POST | `/api/integrations/apple/app-store-server-notifications/v2` | Anonymous + JWS 验签 | 接收 App Store Server Notifications V2 |

上述移动端响应时间统一为带 UTC offset 的 ISO 8601。稳定机器错误码、请求响应示例和兼容期细节以 `docs/integration-guide.md` 为准。

## 10. 后端权限实现

### 10.1 JWT 载荷

可包含：

- `sub`
- `user_name`
- `is_super`
- `site_ids`
- `permissions`

### 10.2 接口权限声明

```csharp
[Authorize]
[Permission("Blog.Article.Delete")]
[HttpDelete("{id}")]
public IActionResult Delete(long id)
{
    return Ok();
}
```

### 10.3 权限缓存策略

- 登录时缓存权限
- 权限变更时清理缓存
- 超级管理员绕过权限缓存判断

### 10.4 前端权限控制

- 动态注册路由
- `v-permission` 控制按钮显示
- 真正的安全兜底仍以后端校验为准

## 11. 初始化数据规则

### 11.1 初始站点

| site_code | 站点名称 |
|-----------|----------|
| `blog` | 个人博客 |
| `ai_image` | GPT 生图网站 |

### 11.2 初始角色

| role_code | 角色名称 | 权限范围 |
|-----------|----------|----------|
| `super_admin` | 超级管理员 | 全部权限 |
| `blog_admin` | 博客管理员 | `blog` 站点全部权限 |
| `ai_operator` | 生图管理员 | `ai_image` 站点全部权限 |

### 11.3 博客权限树

- 内容管理
- 文章管理
  - `Blog.Article.Create`
  - `Blog.Article.Update`
  - `Blog.Article.Delete`
  - `Blog.Article.Publish`
- 媒体管理
  - `Blog.Media.Upload`
  - `Blog.Media.Delete`
- 评论管理
  - `Blog.Comment.Review`
  - `Blog.Comment.Delete`
- 仪表盘
  - `Blog.Dashboard.View`

## 12. 扩展性设计

### 12.1 新增站点流程

1. 在 `sys_site` 新增站点
2. 在 `sys_menu` 配置站点菜单和权限节点
3. 为用户分配站点访问权限
4. 为角色分配菜单权限
5. 新增业务表和业务接口，并继续保留 `site_id`

### 12.2 从单博客接口恢复多站点

如果后续博客模块需要重新支持多站点，有两种可行路线：

- 在现有服务层恢复显式 `siteId` / `siteCode` 入参
- 新增更通用的站点化博客接口，如 `/api/sites/{siteCode}/blog/articles`

当前不建议直接删掉 `site_id` 列，因为这会抹掉未来的扩展基础。

## 13. 非功能设计

### 13.1 安全性

- 密码使用安全哈希
- 登录、注册邮件、兑换和高成本生图入口已实施基线限流；受限响应返回 HTTP 429、`Retry-After` 和 `RATE_LIMITED`
- RefreshToken 默认落 Redis
- 关键操作记录日志
- CORS 严格配置

### 13.2 性能

- 用户权限列表缓存到 Redis
- 菜单按站点缓存
- 博客文章列表利用 `(site_id, status, created_at)` 相关索引

### 13.3 可维护性

- 权限码统一管理
- 响应结构统一
- 审计字段统一
- 站点维度与业务维度解耦

## 14. 演进规划

### 14.1 当前已实现

- 用户、角色、站点、菜单和日志审计
- JWT、Refresh Token family、权限校验与缓存
- 基线 API 限流、`Retry-After` 协议与最小化安全审计
- 博客内容、评论、分类、公开读取和仪表盘
- GPT Image2 与 Nano Banana2 生图、私有媒体、积分结算和提示词过滤
- iOS 法律文档与授权、账户删除、移动端配置、StoreKit 交易和 Apple 服务端通知

### 14.2 后续扩展

- 更完整的博客内容模型和真实前台发布链路
- 新站点快速接入模板
- 数据权限
- 多租户支持

## 15. 结论

本系统采用 `.NET 10 Web API + Vue 3 + MySQL + Redis + JWT` 技术栈，以 `RBAC + 站点维度` 作为统一权限模型。

当前博客模块的取舍是：

- 数据层保留 `site_id`
- 接口层默认固定走 `siteCode=blog`
- 先满足当前“单博客站点后台”的简化需求
- 同时保留未来恢复多站点能力的空间
